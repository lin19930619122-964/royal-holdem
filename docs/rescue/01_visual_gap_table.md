# 01 视觉差距表（V4 §3.1 / §7）

> 对照 V4「成熟牌桌层级 + 独立 SeatView 组件」。严重度 P0(结构性) / P1(明显) / P2(打磨)。
> 负责牌桌 UI 的文件：`src/ui.js`（单体）+ `src/styles.css`。

| 维度 | V4 要求 | 当前状态(文件) | 严重度 | 改造动作 / 涉及文件 | 验收 |
|---|---|---|---|---|---|
| 牌桌分层 | 14 层独立组件(Background/Felt/Seat/Dealer/Community/Pot/BetChip/Hand/Action/TrainingAssistant/Chat/Gift/History/Modal) | `ui.js` 内一把梭 render()，DOM 节点散在 `#table-felt/#seats/#board/#fx-layer` | **P0** | 拆 `src/game/table/` 下分层组件，TableScene 绑定 reducer state | 各层独立文件、可单测 |
| 座位组件 | 独立 `SeatView`，含头像/框/昵称/筹码/本轮下注/筹码堆/状态文字/倒计时环/D/SB-BB/牌背/弃牌态/胜利光圈/最佳五张/连胜标/托管/气泡挂点 | `buildSeats()` 直接拼 DOM；已含 头像/昵称/筹码/下注/倒计时环/D/SB-BB/最佳5张高亮/胜利/气泡 | **P1** | 抽 `SeatView.js`；补「下注筹码堆视图」「连胜小标」「托管/离座态」 | 9/6 人桌座位组件齐全 |
| 桌面质感 | 椭圆桌、桌沿、阴影、高光、材质、纵深、舞台光 | 木纹金边椭圆 + 场景背景(已有) | P2 | 增强材质/纵深(CSS/Canvas) | 非平面背景 |
| 公共牌层 | 翻牌连翻、转河单翻、脉冲、最佳五张高亮 | 有发牌动画 + 最佳5张高亮(`hl5`) | P2 | 接入 GameFeel 统一时序 | 三街动画节奏正确 |
| 底池/边池层 | 主池+边池分别显示 + 数字滚动 | 单一 `#pot-amount`，**无边池可视化**、无数字滚动 | **P1** | `PotView/SidePotView` + 数字滚动 tween | 多人 all-in 时边池可见 |
| 下注筹码层 | 每玩家座位前筹码堆 | 仅 `bet-tag` 数字 | **P1** | `ChipView` 座位前筹码堆 | 下注显示为筹码堆 |
| 操作区 | 按 LegalActions 动态 + 滑杆 + 1/3·1/2·2/3·1池·全下 + 微调 + 危险态 | 有动态按钮 + 滑杆 + ½/⅔/1池 + 精确输入 | P2 | 补 1/3 池、+/- 微调、危险态样式 | 快捷尺度齐全 |
| 9 人布局 | 归一化坐标，不写死像素 | ✅ `SEAT_LAYOUTS[9]` 归一化 + scale | — | — | 已满足 |
| 大厅视觉 | 卡片式入口、身份区、进度、轻动效 | ✅ 已是卡片式 + 开屏 Canvas | P2 | 迁移后保持 | — |
| 启动/登录 | 舞台聚光/漂浮粒子/进度/本地档案 | 有 Canvas 开屏；**无独立 LoginScene**(本地无登录) | P2 | 可加轻量 Login 场景说明「数据仅本机」 | 有进入流程 |

**结论**：视觉**内容**大多具备，但**结构**是单体而非分层（P0），且缺「边池可视化 / 座位筹码堆 / 数字滚动」（P1）。返工重点是 Phase 3 把牌桌拆成分层组件并补这三项。
