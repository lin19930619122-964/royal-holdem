# 升级路线图 —— 按 Phase 0–8（分阶段、每阶段可运行）

> 工作纪律（贯穿全程）：①改动前先列将改文件；②改动后跑测试或说明无法运行的原因；③不删可用功能(除非先给替代方案)；④不堆单文件；⑤不假按钮；⑥不"后续可扩展"逃避核心；⑦不生成未接入的资源/配置；⑧不写大段无用注释；⑨不抄参考 IPA 资源；⑩不用"差不多"验收。
> 约束：不接真钱/充值/提现/广告/风控/上报/麦克风；本地训练筹码；清洁室原创。

每阶段交付物 + 验收标准 + 现状：

---

## Phase 0 — 审计与清洁室规格 ✅ 完成（本次）
- 交付：`docs/` 六文档（ipa-inventory / product-map / cleanroom-abstraction / current-project-audit / gap-matrix / rebuild-roadmap）。
- 验收：文档反映**当前真实状态**（40 提交后），不改任何功能代码。
- 现状：✅ 本阶段产出；前三份(IPA事实/功能地图/清洁室规格)复核仍准确，后三份已刷新到当前。

## Phase 1 — 规则核心与测试 ✅ 核心已建，待统一
- 交付：`src/core/poker/` 11 模块 reducer 核心 + `test-core.js`。
- 已达成：52牌/种子可复现/FY 洗牌/2-9人/盲注前注/翻前→河→摊牌/六类动作/最小加注+短码全下/有效筹码/全下跑牌/边池/多人摊牌/平分/7选5/轮子/皇家/完整日志；**test-core 57 项全绿**。
- **遗留(关键)**：现行 UI 仍用旧 `game.js`。Phase 1 收尾 = 让 reducer 成为唯一事实源，下一阶段接入。
- 验收：`node test-core.js` 全绿（✅）。

## Phase 2 — TableScene 重建（基于 reducer 渲染）
- 将改文件（预告）：新增 `src/core/poker/` 适配/选择器(如 `selectors.js`)；`src/table/`（可选）渲染层；改 `ui.js` 牌桌渲染读 reducer state（不直接改筹码/底池/牌堆）。
- 交付：9 座/公共牌/手牌/底池/下注区/操作区/训练提示，全部由 `state` 渲染；操作按钮经 `LegalActions` 门控。
- 验收：进桌渲染一手静态局正确；`npm test` 绿；不删现有单机可玩性（先并存再切换）。

## Phase 3 — 完整牌局循环（reducer 驱动）
- 将改文件：`ui.js` 牌桌控制器改为 dispatch(START_NEXT_HAND/DEAL_*/PLAYER_ACTION/SHOWDOWN)，按 `state.awaitingDeal` 自动跑牌；AI 决策读新 state（适配层）。
- 交付：进桌→一手→下一手全程经 reducer；旧 `game.js` 下线（保留 git 历史）。
- 验收：人机对战完整可玩、筹码守恒、SNG 仍工作；引擎压测改跑 core；`npm test` 绿。

## Phase 4 — AI Bot 与策略训练（适配新核心）
- 将改文件：`ai.js`（读 reducer state 的 ctx 适配）、训练提示/复盘接 core 日志。
- 交付：Bot 不死板（已有难度/剥削）、训练建议能解释（已有，迁移后保持）。
- 验收：鲨鱼难度 EV 优于基线；复盘"建议X/你选Y"正确；`npm test` 绿。

## Phase 5 — 大厅与成长（已基本完成，巩固）
- 现状：赛季/段位/成就/任务/活动/邮件/金库/礼包/榜/商店/教程/联机社交/持久化好友俱乐部 ✅。
- 本阶段：迁移后回归确认大厅与牌桌数据一致；占位面板若有则补实或明确移除。
- 验收：大厅各入口可用、发奖原子、`npm test` 绿。

## Phase 6 — 教程与复盘（已强，增强）
- 现状：新手教程、复盘+错误分析+最优建议、牌型图鉴 ✅。
- 本阶段（可选增强）：交互式高亮指引、逐街动画回放（基于 core 日志）。
- 验收：复盘逐决策对错+建议来自 core 权威日志。

## Phase 7 — 动画/音频/趣味（已强，打磨）
- 现状：烈焰/庆祝/座驾/通告/升级/合成音/方言/开屏/无障碍 ✅。
- 原则：不喧宾夺主（赢牌多特效已分层错峰）。
- 验收：低动效设备降级生效；无明显掉帧。

## Phase 8 — 性能、适配与打磨
- 将改文件：按真机反馈定（CSS 适配 / 可选胜率 Worker / 资源体积）。
- 交付：移动端流畅、9 机型适配、体积红线(<参考)。
- 验收：真机帧率与输入延迟可接受；安全区正确；体积达标。

---

## 执行顺序与当前位置
**Phase 0 ✅ → 现处 Phase 1 收尾，准备进入 Phase 2/3（把 UI 接到 reducer 核心，统一两套规则实现）**。
其余 Phase 4–7 的目标功能多已实装，迁移后做等价回归；Phase 8 待真机实测。

> 下一步具体动作：Phase 2/3 = 在 `core/poker` 之上加只读 selectors + 一个 reducer 驱动的牌桌控制器，**与现行 game.js 牌桌并存**，跑通后再切换、淘汰旧引擎。全程每步 `npm test`。
