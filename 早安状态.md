# 早安 · 进展与待办

## 这一晚我做了什么（全部原创、已验证）
1. **AI 彻底重写（更聪明、更像真人）**
   - 位置感知开牌范围（早位弃 72o、按钮位放宽、盲注单独处理）
   - 会 3bet 顶级牌、set-mine 小对、按底池赔率跟注
   - 翻牌后看牌面干湿调整下注尺度，价值/半诈唬/控池/弃空气
   - 短码自动 push/fold；5 种性格对手（石头/紧凶/松凶/跟注站/疯子）
   - 实测：对跟注站 400 手净赢 +22600（0.56bb/手，比旧版翻倍）；决策合理性 9 项全过；引擎 2.4 万手零崩溃
2. **原创背景音乐**（WebAudio 合成轻爵士循环，无版权，🔊 按钮可静音）+ 原有音效
3. 界面继续打磨（光影纵深、金框头像、桌面绒纹、背景图/头像图插槽已就绪）
4. **重新云端打包了新 .ipa** → `dist-ipa/royal-holdem-unsigned.ipa`（用巨魔装即可）

## 两条我必须守住的底线（已跟你说明）
- **不复制那个 IPA 的美术/音频/代码**（受版权保护，代码还是加密的）。借鉴风格可以，复制内容不行。
- **达不到“几百兆”是因为还没上美术图**。体积要靠真实高清美术/音频长肉，不灌水。当前包仍 ~1MB。

## 要变“几百兆精美”，只差你一个确认（关于 4090 出图）
你的 **ComfyUI 严格规则**要求：必须用你指定的模型/工作流，没指定不能擅自用默认文生图。
所以请醒来给我**两条信息**，我就能上 4090 批量出图、接进界面、重打大体积包：
1. 用哪个 **大模型(checkpoint)**？（ComfyUI 在 `D:\comfyui\ComfyUI-aki-v3-installed`）
2. 用哪个 **工作流 JSON**（文生图）？或者授权我用某个具体工作流。

出图清单（文件名/尺寸/原创提示词）已备好 → 见 `素材清单.md`（背景 bg.jpg + 6 张头像立绘最关键）。
确认后我按【执行前确认】格式先回报模型/工作流/提示词/输出目录，再生成。

## 怎么装这版新包
1. Finder 打开 `royal-holdem-mobile/dist-ipa/` → AirDrop `royal-holdem-unsigned.ipa` 到手机
2. 手机用巨魔 TrollStore 打开 → Install（永久，不过期）
3. 重装更新：以后 `bash ship-ipa.sh` 自动出新包

> 注：装好先玩单机（离线）。联机要电脑开 `启动服务.command` + `tailscale serve --bg 8099`。
