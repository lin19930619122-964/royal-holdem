# 云端打包皇室德州 → 巨魔安装：从头到尾

目标：不在本机装 Xcode，用 GitHub 云端 Mac 编译出**未签名 .ipa**，再用 iPhone 上的**巨魔 TrollStore** 永久安装。

---

## 我已经替你做好的（无需你管）
- 安装了 `gh`（GitHub 命令行）到 `~/.local/bin/gh`
- 项目已 `git` 初始化并提交（`main` 分支）
- 写好云端编译工作流 `.github/workflows/ios-ipa.yml`
- 写好一键脚本 `ship-ipa.sh`（建仓库→推送→编译→下载 .ipa）

**你全程只需要做第 1 步（登录），其余可以让我跑。**

---

## 第 1 步：登录 GitHub（唯一必须你做的）

> 需要一个 GitHub 账号。没有就先去 github.com 用邮箱注册一个（免费，2 分钟）。

打开 Mac 的「终端」App，粘贴运行：
```bash
export PATH="$HOME/.local/bin:$PATH"
gh auth login
```
按提示用方向键 + 回车依次选：
1. `GitHub.com`
2. `HTTPS`
3. Authenticate Git with your GitHub credentials? → `Yes`
4. `Login with a web browser`
5. 屏幕给出一次性代码（如 `ABCD-1234`）→ 回车自动开浏览器 → 粘贴代码 → Authorize

看到 `✓ Logged in as 你的用户名` 即成功。

---

## 第 2 步：一键编译 + 下载 .ipa

**方式 A（推荐，让我跑）**：登录成功后回我一句"好了"，我直接执行 `ship-ipa.sh`，跑完把 .ipa 路径给你。

**方式 B（你自己跑）**：终端运行
```bash
cd "/Users/linlin/cc  工作站/royal-holdem-mobile"
export PATH="$HOME/.local/bin:$PATH"
bash ship-ipa.sh
```
脚本会自动：
1. 在你账号下建公开仓库 `royal-holdem`
2. 推送代码
3. 触发云端 macOS 编译（约 5–10 分钟，脚本会实时显示进度）
4. 下载产物到 `dist-ipa/royal-holdem-unsigned.ipa`

完成后终端会打印 .ipa 的完整路径。

> 想改联机服务器地址：`bash ship-ipa.sh royal-holdem 你的地址`

**方式 C（纯网页，不用脚本）**：
1. `gh repo create royal-holdem --public --source=. --push`
2. 打开 `https://github.com/你的用户名/royal-holdem/actions`
3. 选「构建未签名 iOS IPA」→ Run workflow
4. 跑完进入该次运行 → 底部 **Artifacts** → 下载 `royal-holdem-unsigned-ipa`（zip，解压得 .ipa）

---

## 第 3 步：.ipa 传到 iPhone
- **AirDrop**：Finder 里右键 .ipa → 共享 → 隔空投送给你的 iPhone（最快）
- 或微信「文件传输助手」发给手机

---

## 第 4 步：巨魔 TrollStore 安装
1. iPhone 收到 .ipa 后，在弹出的「打开方式」里选 **TrollStore**（或先存到「文件」App，再到 TrollStore 里 Import）
2. TrollStore 显示 App 信息 → 点 **Install**
3. 主屏出现「皇室德州」，点开即玩。**永久有效，不会过期、不用重签。**

> 巨魔会自动 ad-hoc 签名，未签名 .ipa 正合适。包名 `com.royalholdem.app`。

---

## 玩法说明
- **单机**：完全内置，离线可玩（金币/签到/商店/兑换码/皮肤/动画全在）。
- **联机（真人对战）**：App 连的是你电脑上的服务器（已写入 `m5.tail5255b4.ts.net`）。
  玩联机时：电脑要开着服务（双击 `启动服务.command` + `tailscale serve --bg 8099`），手机在同一 Tailnet。

---

## 常见问题
- **gh auth login 卡住/打不开浏览器**：选 `Login with a web browser` 后手动把终端给的 URL 复制到浏览器。
- **Actions 没自动跑**：本工作流是手动触发（Run workflow），不是 push 自动触发。
- **编译失败**：`gh run view <运行ID> --log-failed` 看日志；常见是 `cap add ios` 的 CocoaPods 问题，云端一般已预装，重跑一次多半好。
- **macOS 编译分钟数**：用**公开仓库** Actions 免费无限；私有仓库 macOS 有配额，所以脚本默认建公开仓库。
- **巨魔装完打不开/闪退**：多为联机找不到服务器；先玩单机确认，联机记得开电脑服务 + Tailscale。
